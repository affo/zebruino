// pin dei sensori di prossimità
int proximity1, proximity2;
// pin dei led
int led1, led2, led_voice;
// pin del microfono
int mic;


void setup(){
  pinMode(proximity1, INPUT);
  pinMode(proximity2, INPUT);
  pinMode(led1, OUTPUT);
  pinMode(led2, OUTPUT);
  pinMode(led_voice, OUTPUT);
  pinMode(mic, INPUT);
}

void loop(){
  
}
